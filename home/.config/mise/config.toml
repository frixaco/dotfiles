[tools]
node = 'lts'
go = 'latest'
bun = 'latest'
deno = 'latest'
uv = 'latest'
zig = 'latest'

[tasks."lsp:install"]
description = "Install LSP tools (mise + go + uv + rustup)"
run = '''
#!/usr/bin/env bash
set -euo pipefail

mise install

bun pm cache rm -g
bun install -g \
  @astrojs/language-server@latest \
  @johnnymorganz/stylua-bin@latest \
  @olrtg/emmet-language-server@latest \
  @tailwindcss/language-server@latest \
  @taplo/cli@latest \
  @typescript/native-preview@beta \
  eslint@latest \
  oxfmt@latest \
  oxlint@latest \
  prettier@latest \
  shellcheck@latest \
  typescript-language-server@latest \
  vscode-langservers-extracted@latest

go install golang.org/x/tools/gopls@latest
go install golang.org/x/tools/cmd/goimports@latest
go install mvdan.cc/sh/v3/cmd/shfmt@latest
uv tool install --upgrade ruff
uv tool install --upgrade ty
uv tool install --upgrade clang-format
rustup component add rust-analyzer --toolchain stable
'''
run_windows = [
  "mise install",
  "bun install -g @astrojs/language-server@latest @johnnymorganz/stylua-bin@latest @olrtg/emmet-language-server@latest @tailwindcss/language-server@latest @taplo/cli@latest @typescript/native-preview@beta eslint@latest oxfmt@latest oxlint@latest prettier@latest shellcheck@latest typescript-language-server@latest vscode-langservers-extracted@latest",
  "go install golang.org/x/tools/gopls@latest",
  "go install golang.org/x/tools/cmd/goimports@latest",
  "go install mvdan.cc/sh/v3/cmd/shfmt@latest",
  "uv tool install --upgrade ruff",
  "uv tool install --upgrade ty",
  "uv tool install --upgrade clang-format",
  "rustup component add rust-analyzer --toolchain stable",
]

[tasks."lsp:doctor"]
description = "Check LSP binaries on PATH"
run = '''
#!/usr/bin/env bash
set -euo pipefail
bins=(
  rust-analyzer
  gopls
  goimports
  stylua
  clang-format
  ruff
  ty
  emmet-language-server
  vscode-eslint-language-server
  vscode-html-language-server
  vscode-css-language-server
  tailwindcss-language-server
  typescript-language-server
  tsgo
  astro-ls
  oxfmt
  oxlint
  taplo
  shfmt
  shellcheck
  prettier
  eslint
)
for bin in "${bins[@]}"; do
  if path="$(command -v "$bin")"; then
    printf "%s -> %s\n" "$bin" "$path"
  else
    printf "%s -> NOT FOUND\n" "$bin" >&2
    exit 1
  fi
done
'''
run_windows = [
  "where rust-analyzer",
  "where gopls",
  "where goimports",
  "where stylua",
  "where clang-format",
  "where ruff",
  "where ty",
  "where emmet-language-server",
  "where vscode-eslint-language-server",
  "where vscode-html-language-server",
  "where vscode-css-language-server",
  "where tailwindcss-language-server",
  "where typescript-language-server",
  "where tsgo",
  "where astro-ls",
  "where oxfmt",
  "where oxlint",
  "where taplo",
  "where shfmt",
  "where shellcheck",
  "where prettier",
  "where eslint",
]

[tasks."lsp:sync"]
description = "Install then verify LSP stack"
run = [
  { task = "lsp:install" },
  { task = "lsp:doctor" },
]

[tasks."ai:install"]
description = "Install/update AI coding agents"
run = '''
#!/usr/bin/env bash
set -euo pipefail

if ! command -v amp &> /dev/null; then
    curl -fsSL https://ampcode.com/install.sh | bash
fi


mise install
bun pm cache rm -g
bun install -g \
  @earendil-works/pi-coding-agent@latest \
  @oh-my-pi/pi-coding-agent@latest \
  @openai/codex@latest \
  agent-browser@latest \
  opencode-ai@latest

amp update
pi update --extensions
curl -fsSL https://raw.githubusercontent.com/dmtrKovalenko/fff.nvim/main/install-mcp.sh | bash
'''

run_windows = [
  "pwsh -Command \"if (!(Get-Command amp -EA SilentlyContinue)) { irm https://ampcode.com/install.ps1 | iex } else { amp update }\"",
  "mise install",
  "bun install -g @earendil-works/pi-coding-agent@latest @oh-my-pi/pi-coding-agent@latest @openai/codex@latest agent-browser@latest opencode-ai@latest",
  "pwsh -NoProfile -Command \"irm https://raw.githubusercontent.com/dmtrKovalenko/fff.nvim/main/install-mcp.ps1 | iex\"",
]

[tasks."ai:link"]
description = "Link shared AI agent instructions and configure the Codex browser MCP"
run = '''
#!/usr/bin/env bash
set -euo pipefail

mkdir -p \
  ~/.config/opencode \
  ~/.pi/agent \
  ~/.config/amp \
  ~/.codex

ln -sfn ~/.config/AGENTS.md ~/.config/opencode/AGENTS.md
ln -sfn ~/.config/AGENTS.md ~/.pi/agent/AGENTS.md
ln -sfn ~/.config/AGENTS.md ~/.config/amp/AGENTS.md
ln -sfn ~/.config/AGENTS.md ~/.codex/AGENTS.md

# Reuse the existing signed-in browser, as the other agents do.
codex mcp add chrome-devtools -- npx -y chrome-devtools-mcp@latest --no-usage-statistics --browserUrl http://127.0.0.1:9222

'''
run_windows = "mise run ai:link:windows"

[tasks."ai:link:windows"]
hide = true
shell = "pwsh -NoProfile -Command"
run = '''
$ErrorActionPreference = "Stop"

$dirs = @(
  (Join-Path $HOME ".config\opencode"),
  (Join-Path $HOME ".pi\agent"),
  (Join-Path $HOME ".config\amp"),
  (Join-Path $HOME ".codex")
)
New-Item -ItemType Directory -Force -Path $dirs | Out-Null

$agentSource = Join-Path $HOME ".config\AGENTS.md"
$agentLinks = @(
  (Join-Path $HOME ".config\opencode\AGENTS.md"),
  (Join-Path $HOME ".pi\agent\AGENTS.md"),
  (Join-Path $HOME ".config\amp\AGENTS.md"),
  (Join-Path $HOME ".codex\AGENTS.md")
)
foreach ($link in $agentLinks) {
  Remove-Item -Force -LiteralPath $link -ErrorAction SilentlyContinue
  New-Item -ItemType SymbolicLink -Path $link -Target $agentSource | Out-Null
}

codex mcp add chrome-devtools -- npx -y chrome-devtools-mcp@latest --no-usage-statistics --browserUrl http://127.0.0.1:9222
if ($LASTEXITCODE -ne 0) { throw "Failed to configure Codex browser MCP" }

'''

[tasks."ai:doctor"]
description = "Check AI agent binaries on PATH"
run = '''
#!/usr/bin/env bash
bins=(amp opencode codex pi omp)
missing=0
for bin in "${bins[@]}"; do
  if path="$(command -v "$bin" 2>/dev/null)"; then
    printf "%s -> %s\n" "$bin" "$path"
  else
    printf "%s -> NOT FOUND\n" "$bin" >&2
    missing=1
  fi
done
exit "$missing"
'''
run_windows = "mise run ai:doctor:windows"

[tasks."ai:doctor:windows"]
hide = true
shell = "pwsh -NoProfile -Command"
run = '''
$paths = @(
  (Join-Path $HOME ".amp\bin"),
  (Join-Path $HOME ".local\bin"),
  (Join-Path $HOME ".bun\bin"),
  (Join-Path $HOME ".opencode\bin"),
  (Join-Path $env:LOCALAPPDATA "fff-mcp\bin")
)
$env:Path = (($paths + ($env:Path -split ";")) | Where-Object { $_ } | Select-Object -Unique) -join ";"

foreach ($bin in @("amp", "opencode", "codex", "pi")) {
  $cmd = Get-Command $bin -ErrorAction SilentlyContinue
  if ($cmd) {
    Write-Output "$bin -> $($cmd.Source)"
  } else {
    Write-Output "$bin -> NOT FOUND"
    $missing += $bin
  }
}
if ($missing) { throw "Missing AI agents: $($missing -join ', ')" }
'''

[tasks."ai:sync"]
description = "Install, link, then verify AI agents"
run = [
  { task = "ai:install" },
  { task = "ai:link" },
  { task = "ai:doctor" },
]

# Tracking must be declared globally so the watcher sees it from any directory.
[settings]
experimental = true
history.sync = "manual"

[dotfiles]
"~/.config/amp/settings.json" = { mode = "track" }
"~/.config/btop/btop.conf" = { mode = "track" }
"~/.config/opencode/tui.json" = { mode = "track" }
"~/.config/mise/config.toml" = { mode = "track" }
"~/.omp/agent/config.yml" = { mode = "track" }
"~/.config/nvim/init.lua" = { mode = "track" }
"~/.config/nvim/lua" = { mode = "track" }
"~/.config/nvim/stylua.toml" = { mode = "track" }
"~/.config/aerospace/aerospace.toml" = { mode = "track" }
"~/.config/fish/conf.d/00-mise.fish" = { mode = "track" }
"~/.config/fish/themes/cyberdream.theme" = { mode = "track" }
"~/.config/ghostty/themes/cyberdream" = { mode = "track" }
"~/.config/kitty/kitty.conf" = { mode = "track" }
"~/.config/kitty/cyberdream.conf" = { mode = "track" }
"~/.config/btop/themes/cyberdream.theme" = { mode = "track" }
"~/.config/bat/config" = { mode = "track" }
"~/.config/bat/themes/cyberdream.tmTheme" = { mode = "track" }
"~/.config/delta/themes/cyberdream.gitconfig" = { mode = "track" }
"~/.config/git/ignore" = { mode = "track" }
"~/.config/jjui/config.toml" = { mode = "track" }
"~/.config/jjui/themes/cyberdream-dark.toml" = { mode = "track" }
"~/.config/lazygit/config.yml" = { mode = "track" }
"~/.config/opencode/opencode.json" = { mode = "track" }
"~/.config/opencode/themes/cyberdream.json" = { mode = "track" }
"~/.config/starship.toml" = { mode = "track" }
"~/.config/yazi/init.lua" = { mode = "track" }
"~/.config/yazi/theme.toml" = { mode = "track" }
"~/.config/yazi/yazi.toml" = { mode = "track" }
"~/.config/yazi/flavors/cyberdream.yazi/flavor.toml" = { mode = "track" }
"~/.config/AGENTS.md" = { mode = "track" }
"~/.config/agent-workflows" = { mode = "track" }
"~/.codex/config.toml" = { mode = "track" }
"~/.pi/agent/mcp.json" = { mode = "track" }
"~/.pi/agent/settings.json" = { mode = "track" }
"~/.dotfiles/home/.zshrc" = { mode = "track" }
"~/.dotfiles/home/.gitconfig" = { mode = "track" }
"~/.dotfiles/home/.config/fish/config.fish" = { mode = "track" }
"~/.dotfiles/home/.config/ghostty/config" = { mode = "track" }
"~/.dotfiles/home/.config/1Password/ssh/agent.toml" = { mode = "track" }
"~/.dotfiles/home/stuff/code/vbrato/.gitconfig-vbrato" = { mode = "track" }
"~/.dotfiles/mise.toml" = { mode = "track" }

[bootstrap.services.mise-history]
builtin = "history-watch"
